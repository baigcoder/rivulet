{
  "buildCommand": "ls -la && find . -maxdepth 2 -type d && mkdir -p output && cp -r public/. output/ 2>/dev/null || echo 'no public dir'",
  "outputDirectory": "output",
  "rewrites": [{ "source": "/", "destination": "/landing.html" }]
}
